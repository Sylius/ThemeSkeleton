{#

    Available variables:

    * "user" - The ShopUser entity entity;
    * "user.passwordResetToken" - The token required to change the password;
    * "url" - Resetting URL.

#}

{% set url = channel.hostname is not null ? 'http://' ~ channel.hostname ~ path('sylius_shop_password_reset', {'token': user.passwordResetToken}) : url('sylius_shop_password_reset', {'token': user.passwordResetToken}) %}

{% block subject %}
{% endblock %}

{% block body %}
    {% autoescape %}
    {% endautoescape %}
{% endblock %}
